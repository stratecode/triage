[project]
name = "triage-slack-bot"
version = "0.1.0"
description = "Slack bot service for TrIAge"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"
